mixin menu-title(data = {})
  - const title = data.title;
  - const description = data.description || false;

  .menu-title&attributes(attributes)
    h2.title.title--h3.title--pink.menu-title__title!= title

    if (description)
      span.menu-title__description!= description
